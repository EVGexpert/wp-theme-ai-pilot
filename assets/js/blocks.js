/**
 * AIPilot Demo — Editor registration (deprecated).
 *
 * All 14 custom blocks have been migrated to the aipilot-demo-blocks plugin
 * (src/*, built with @wordpress/scripts). This file is no longer loaded.
 * Kept for reference only — safe to delete after confirming everything works.
 */
